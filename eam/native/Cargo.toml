[package]
name = "eam-runtime"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
publish = false

[dependencies]
libc = "0.2"
serde_json = "1"
toml = "0.9"
rusqlite = { version = "0.37", features = ["bundled"] }

[profile.release]
lto = "thin"
strip = true
